<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/stripes"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.dori.failsafeforreal.MainSettingsPage"
    android:id="@+id/settingslayout"
    android:alpha="150">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="140dp"
        android:text="Application Settings"
        android:id="@+id/applicationButton"
        android:layout_above="@+id/accountButton"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textSize="45dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="140dp"
        android:text="Account Settings"
        android:id="@+id/accountButton"
        android:layout_above="@+id/helpButton"
        android:layout_alignLeft="@+id/applicationButton"
        android:layout_alignStart="@+id/applicationButton"
        android:layout_alignRight="@+id/applicationButton"
        android:layout_alignEnd="@+id/applicationButton"
        android:textSize="45dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="140dp"
        android:text="Help and Support"
        android:id="@+id/helpButton"
        android:layout_above="@+id/termsButton"
        android:layout_alignLeft="@+id/accountButton"
        android:layout_alignStart="@+id/accountButton"
        android:layout_alignRight="@+id/accountButton"
        android:layout_alignEnd="@+id/accountButton"
        android:textSize="45dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="140dp"
        android:text="Terms and Conditions"
        android:id="@+id/termsButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignLeft="@+id/helpButton"
        android:layout_alignStart="@+id/helpButton"
        android:textSize="45dp" />
</RelativeLayout>
